{
  "name": "NoVA Kernel (begonia) devcontainer",
  "dockerFile": "docker/Dockerfile",
  "context": ".",
  "workspaceFolder": "/workdir",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workdir,type=bind,consistency=delegated"
  ],
  "remoteUser": "builder",
  "extensions": [
    "ms-vscode.cpptools",
    "ms-vscode.cmake-tools"
  ],
  "postCreateCommand": "git config --global user.email \"you@example.com\" || true && git config --global user.name \"You\" || true"
}
